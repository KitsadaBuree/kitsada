# Server
NODE_ENV=production
PORT=3000

# DB (คอนเทนเนอร์คุยกันผ่านชื่อ service)
DB_HOST=db
DB_PORT=3306
DB_USER=morning
DB_PASSWORD=kitsada
DB_DATABASE=restaurant_db

# App secrets / config
CLOUDINARY_URL=cloudinary://<key>:<secret>@<cloud_name>
JWT_SECRET=<strong-random-hex>
# ลูกค้า (mobile) เปิดที่ :3001 ในเครื่องเดียวกัน
NEXT_PUBLIC_CUSTOMER_ORIGIN=http://localhost:3001
